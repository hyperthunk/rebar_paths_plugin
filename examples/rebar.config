
{path_opts, [
    {xref, [
        {append, ["../ebin"]}
    ]}
]}.

{xref_checks, [undefined_function_calls]}.

{deps, [
    {rebar_paths_plugin, ".*",
        {git, "../../", "master"}}
]}.


{plugins, [rebar_plugin_manager, rebar_paths_plugin]}.
{plugin_dir, "deps/rebar_plugin_manager/src"}.

